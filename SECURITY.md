# 安全策略（Security Policy）

## 支持的版本

| 版本线 | 支持状态 |
| --- | --- |
| main（2.0 开发线） | ✅ 接收安全修复 |
| feature/* 分支 | ⚠️ 随开发推进修复 |
| 1.x 历史标签 | ❌ 不再单独维护，建议升级 |

## 报告漏洞

LPY-DOS 是教学向操作系统，主要运行在 QEMU 等模拟器中。以下情况请务必报告：

1. **引导链缺陷**：引导扇区 / LOADR.SYS / MBR 对恶意或损坏的磁盘镜像
   处理不当（越界读写、死循环、不受控重启——本项目验收标准明确禁止
   死机与自动重启，必须受控挂机并显示错误）。
2. **内存越界**：INT 21h 服务、文件系统、EXEC 对非法参数（坏簇链、
   越界句柄、非法路径）的边界处理缺陷。
3. **真实硬件风险**：任何可能导致对真实软盘/硬盘镜像之外介质误写入的路径。

### 报告方式

- 通过 GitHub **Security Advisories**（Security → Report a vulnerability）私下报告；
  或开 ISSUE 并将技术细节最小化。
- 报告请附：复现步骤、镜像构造方式（可参考 `tests/mkhd.ps1`）、
  QEMU 现场取证（截图 / 寄存器 / 内存转储）。

### 响应约定

- 维护者确认后会在 ISSUE 中同步处置进度；
- 修复以 `fix:` 提交交付，并在 `operation-record-document.md` 登记；
- 报告者将被列入修复提交的致谢中（如愿意）。

## 范围之外

- 依赖的第三方工具（QEMU / FASM / PowerShell）自身的问题请向对应上游报告。
- 本项目面向教学与实验环境，**不承诺**在真实生产环境中作为日常操作系统使用。
