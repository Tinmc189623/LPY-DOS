# 核心任务：一次性完成自研操作系统 "Nexsteaduser OS" 全栈开发
## 关键约束（违反将导致任务失败）
1. 内核必须100%自研，**绝对禁止使用/套用任何现有操作系统代码、框架或内核（如Linux、Windows、Redox、Zircon等）**，所有底层逻辑必须从0到1编写，违者精准打死
2. 语言分工：Rust负责内核/底层（内存管理、进程调度、文件系统、驱动等），Python负责命令行终端与用户交互层
3. 必须包含完整图形化界面（GUI），支持窗口管理、鼠标/键盘交互、基础图形渲染
4. 最终交付可引导ISO镜像，支持BIOS/UEFI双启动
5. 完成后执行**1800遍→2100遍→3400遍→100000800遍**递进式全链路优化，覆盖性能、内存、稳定性、启动速度
6. 一次性完成，**禁止分阶段/延期交付**（拒绝"一个月/两年"的开发周期）

## 系统架构要求（必须严格执行）
### 1. 内核层（Rust实现）
- 架构：微内核设计，模块化组件，支持动态加载
- 核心组件（全部自研）：
  - 启动引导程序（Bootloader）：实现实模式→保护模式→长模式切换，初始化硬件
  - 内存管理：物理内存分页、虚拟内存映射、内存安全检查、内存泄漏防护
  - 进程管理：进程创建/销毁、调度器（支持抢占式多任务）、IPC机制
  - 文件系统：自研日志型文件系统（NexFS），支持目录、权限、硬链接/软链接
  - 设备驱动：VGA显示驱动、PS/2键盘/鼠标驱动、ATA硬盘驱动、PCI总线枚举
  - 中断处理：PIC/APIC管理、中断向量表、异常处理机制
  - 系统调用：自定义syscall接口，支持Python终端调用内核服务

### 2. 图形界面层（Rust实现）
- 显示服务器：自研 compositor，支持2D渲染、窗口合成、双缓冲防闪烁
- 窗口管理器：支持窗口移动、缩放、最小化/最大化、多桌面
- 基础组件：按钮、文本框、标签、菜单、对话框等控件库
- 图形API：提供2D绘图接口，支持颜色、字体、位图渲染

### 3. 用户交互层（Python实现）
- 命令行终端：支持Python REPL，内置命令（文件操作、进程管理、系统信息等）
- 系统API绑定：通过FFI调用Rust内核服务，封装为Python友好接口
- 应用程序：文件管理器、文本编辑器、终端模拟器等基础应用

## 技术实现规范
1. Rust代码规范：
   - 使用 nightly 版 Rust，启用 no_std 环境
   - 严格内存安全，禁止unsafe代码（除非必须且有详细注释）
   - 采用 cargo 构建系统，分模块组织代码
   - 支持 x86_64 架构，兼容主流硬件

2. Python集成规范：
   - 嵌入RustPython解释器（完全自研适配层，不使用现有绑定）
   - 实现Python↔Rust通信桥，支持数据类型转换
   - 终端支持ANSI转义序列，彩色输出，历史记录

3. ISO制作规范：
   - 包含MBR/UEFI引导程序，支持传统BIOS与现代UEFI启动
   - 文件系统采用ISO9660+Rock Ridge扩展
   - 压缩内核镜像，优化启动速度
   - 提供虚拟机测试脚本（QEMU）

## 优化标准（每轮优化必须达成）
1. 1800遍：基础性能优化，修复内存泄漏，启动时间≤5秒
2. 2100遍：并发优化，进程调度延迟≤1ms，支持≥100个并发进程
3. 3400遍：资源占用优化，空闲内存占用≤10MB，磁盘IO吞吐量提升50%
4. 100000800遍：终极优化，内核代码精简30%，响应时间≤100μs，系统稳定性MTBF≥10000小时

## 交付物清单（缺一不可）
1. 完整源代码（Rust内核+Python终端），组织为清晰目录结构
2. 编译脚本（Cargo.toml、Makefile），一键编译生成ISO
3. 可引导ISO镜像文件（nexsteaduser-os.iso），大小≤200MB
4. 详细技术文档：内核架构、系统调用表、文件系统格式、编译指南
5. 测试报告：功能测试、性能测试、稳定性测试结果

## 执行流程（Solo模式自动执行）
1. 架构设计→模块划分→代码编写→集成测试→图形界面开发→Python终端对接
2. 一次性完成所有开发，**禁止中断或分步提交**
3. 开发完成后自动执行四轮优化，每轮优化后生成优化报告
4. 最终打包ISO镜像，输出所有交付物

## 核心指令
现在立即启动TRAE-CN Solo模式，以GLM-4.7模型执行上述任务，**必须一次性完成，全程自研，不准复用任何外部代码**，否则任务失败。