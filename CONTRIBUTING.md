# 参与 LPY-DOS 开发（Contributing）

感谢关注 LPY-DOS！这是一个运行在 8086 实模式上的教学向 DOS 兼容操作系统，
使用 FASM 汇编与 C 语言编写。提交代码前请务必阅读以下约定。

## 硬性技术约束

1. **指令集**：16 位内核与程序只允许 8086 指令集；386+ 指令只允许出现在
   `src/kernel/arch/i386/` 保护模式扩展及其后续模块中。
2. **语言**：汇编（FASM）与 C，不引入其他语言。
3. **架构原则**：ASR（每个文件只负责一个功能）、DRY、KISS、YAGNI。
   禁止把所有功能压进一个 SYS 文件；系统文件遵循 8.3 命名。
4. **注释**：最少核心注释，注释与代码保持一致。

## 开发环境

- FASM（flat assembler，路径可用环境变量 `LPYDOS_FASM` 指定）
- Windows PowerShell 5.1 或 7+（构建脚本）
- QEMU（`qemu-system-i386`，可选，用于实机验证）
- Python 3（可选，用于 HD 镜像生成）

## 构建与验证

```powershell
pwsh -File .\build.ps1          # 全量构建，产物统一输出到 bin/
```

涉及引导链、内核或文件系统的改动，请用 `tests/` 下的无头 QEMU 辅助做实机验证：

```powershell
pwsh -NoProfile -Command ". ./tests/qemu.ps1; Start-LpyQemu -Image 'bin/LPY-DOS.img'; ..."
```

内核/文件系统改动应当与改动前构建产物做逐字节对比（重构类改动要求二进制零变化），
行为类改动需在 QEMU 中实际操作验证。

## 协作规范（重要）

1. **操作登记**：任何操作（包括成功与失败）必须事先/事后登记在内部操作记录
   （维护者与协作文档，不入公开仓库）。
2. **分支模型**：功能开发在 `feature/*` 分支进行，经作者确认后并入 main。
3. **提交信息**：遵循 Conventional Commits（`feat:` / `fix:` / `refactor:` /
   `build:` / `docs:` / `chore:`），一次提交一个逻辑单元。
4. **版本号**：遵循 `主.次.修订` 语义（主版本=史诗级变更，次版本=重大功能，
   修订=缺陷修复/优化）；唯一来源是根目录 `version.ini`。

## 提交内容检查清单

- [ ] 不包含构建产物（`bin/`、`*.COM`、`*.img` 等均已被 .gitignore 忽略）
- [ ] 不包含调试转储与日志文件
- [ ] 拆分类改动：拆分前后构建产物逐字节一致
- [ ] 行为类改动：QEMU 实机验证通过，取证已登记操作记录
- [ ] 遵守 8086 指令集与项目结构规范

## 提交方式

Fork → 功能分支 → ISSUE/PR 讨论 → 作者审核合并。
欢迎从小改动开始；大规模重构请先在 ISSUE 中与作者对齐方案。
